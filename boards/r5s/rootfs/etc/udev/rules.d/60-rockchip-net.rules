# 60-rockchip-net.rules（R5S 覆盖版 · 空操作）
#
# R5S 的网口命名由 rockchip-r5s-ifrename.service 在 vyos-router 之前、按 driver + PCIe
# 控制器路径确定性完成（eth0=gmac0 WAN、eth1/eth2=两个 RTL8125），不走 VyOS 的 udev
# 预定义命名（VYOS_IFNAME）。
#
# 本文件存在的唯一目的：以同名覆盖 base 里的 RK3528 通用规则（rk_gmac-dwmac→eth1 /
# r8169→eth0），不让它作用到 R5S。否则 base 会给 R5S 的 gmac 设 VYOS_IFNAME=eth1,
# vyos_net_name 随即把“预定义名”写进 /run/udev/vyos/,导致 vyos-interface-rescan 把那串
# 名字当 MAC 解析、抛 netaddr AddrFormatError（启动日志里的 traceback;不致命但难看）。
# 故这里只留空规则,什么都不设。
ACTION!="add",    GOTO="rk_net_end"
SUBSYSTEM!="net", GOTO="rk_net_end"
LABEL="rk_net_end"
